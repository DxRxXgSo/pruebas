version: '3.4'

services:
  catalogdb:
    image: postgres:15
  basketdb:
    image: postgres:15
  distributedcache:
    image: redis

  mongodb:
    image: mongo:7

  basket.api:
    image: ${DOCKER_REGISTRY-}basketapi
    build:
      context: .
      dockerfile: src/Basket/Basket.API/Dockerfile

  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: .
      dockerfile: src/Catalog.API/Dockerfile

  ordering.api:
    image: ${DOCKER_REGISTRY-}orderingapi
    build:
      context: .
      dockerfile: src/Ordering.API/Dockerfile

  pdf.api:
    image: ${DOCKER_REGISTRY-}pdfapi
    build:
      context: .
      dockerfile: src/Pdf.API/Dockerfile

volumes:
  postgres_catalog:
  postgres_basket: